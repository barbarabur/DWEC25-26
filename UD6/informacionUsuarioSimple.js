function obtenerInfoUsuario(idUsuario) {
    return new Promise((resolve, reject) => {
        
        const minDelay = 500; // 0.5 segundos
        const maxDelay = 3000; // 3 segundos
        const delay = Math.floor(Math.random() * (maxDelay - minDelay + 1)) + minDelay;
        
    })
    
}